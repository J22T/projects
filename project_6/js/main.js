function tellStory() {
    //nounArray = querySelector('#noun') //lowercase String split()
    //repeat for adjective and verbs

    const myStory = `Once upon a time there were four ${nounArray[0]}named Flopsy, Mopsy.... etc.`

    // get a reference to the querySelector('#story') = myStory